{"body": {"column":32,"line":30,"wordToComplete":"Locked.\"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var operationPerformed bool\n    var lastValue int\n    var totalDoors int\n    var minOperations int\n    var maxOperations int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &totalDoors)\n    \n    for doorsCounter := 0; doorsCounter < totalDoors; doorsCounter++ {\n        var doorStatus int\n        fmt.Fscan(io, &doorStatus)\n        fmt.Println(\"Door #\", doorsCounter+1)\n        fmt.Println(\"Status: \", doorStatus)\n        if doorStatus == 0 {\n            lastValue = doorStatus\n            fmt.Println(\"Already unlocked, go ahead\")\n            continue\n        }\n        \n        if doorStatus == 1 {\n            maxOperations++\n            fmt.Println(\"Locked.\")\n            if !operationPerformed {\n                lastValue = doorStatus\n                operationPerformed = true\n                minOperations++\n                continue\n            }\n            \n            if lastValue == 1 && doorStatus == 1 && operationPerformed {\n                operationPerformed = false\n                continue\n            }\n        }\n    }\n    \n    fmt.Println(minOperations, maxOperations)\n}\n\n","offset":743,"fileType":"go","requestStartTime":1517910217987}}
{"body": {"column":8,"line":5,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    fmt.\n}\n\n","offset":51,"fileType":"go","requestStartTime":1517912026138}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    fmt.Scan(&n)\n    fmt.\n}\n\n","offset":68,"fileType":"go","requestStartTime":1517912037467}}
{"body": {"column":8,"line":18,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var operationPerformed bool\n    var totalDoors int\n    var minOperations int\n    var maxOperations int\n    var doorsStatus []int\n    doorsStatus = make([]int, totalDoors)\n    \n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &totalDoors)\n    fmt.\n   \n    AllDoors = ReadN(doorsStatus, totalDoors)\n    \n    for totalDoors > 0 {\n        totalDoors--\n        var doorStatus byte\n        fmt.Fscan(io, &doorStatus)\n        //fmt.Println(\"Door #\", totalDoors)\n        //fmt.Println(\"Status: \", doorStatus)\n        \n        if doorStatus == 0 {\n            //fmt.Println(\"Already unlocked, go ahead\")\n            //fmt.Println(\"------\")\n            continue\n        }\n        \n        if doorStatus == 1 {\n            maxOperations++\n            //fmt.Println(\"Locked...\")\n            if !operationPerformed {\n                operationPerformed = true\n                minOperations++\n                //fmt.Println(\"Unlocking\")\n                //fmt.Println(\"------\")\n                continue\n            }\n            \n            if operationPerformed {\n                operationPerformed = false\n                continue\n            }\n        }  \n    }\n    \n    fmt.Println(minOperations, maxOperations)\n}\n\nfunc ReadN(all []int, i, n int) {\n    if n == 0 {\n        return\n    }\n    if m, err := Scan(&all[i]); m != 1 {\n        panic(err)\n    }\n    ReadN(all, i+1, n-1)\n}\n\nfunc Scan(a *int) (int, error) {\n    return fmt.Scan(a)\n}\n\n","offset":327,"fileType":"go","requestStartTime":1517912602413}}
{"body": {"column":17,"line":23,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n    \"bytes\"\n)\n\nfunc main() {\n    //var operationPerformed bool\n    var currentStatus []byte\n    var doorCounter int\n    var totalDoors int\n    var minOperations int\n    var maxOperations int\n    \n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &totalDoors)\n    \n    for doorCounter < totalDoors {\n        fmt.Fscan(io, &currentStatus)\n        fmt.Println(\"Status: \", currentStatus)\n        if bytes.currentStatus != byte(48) && currentStatus != byte(49) {\n            continue\n        }\n        doorCounter++*/\n        \n        fmt.Println(\"Door #\", totalDoors)\n        fmt.Println(\"Status: \", currentStatus)\n        \n        /*\n        if currentStatus == byte(48) {\n            fmt.Println(\"Already unlocked, go ahead\")\n            fmt.Println(\"------\")\n            continue\n        }\n        \n        if currentStatus == byte(49) {\n            maxOperations++\n            fmt.Println(\"Locked...\")\n            if !operationPerformed {\n                operationPerformed = true\n                minOperations++\n                //fmt.Println(\"Unlocking\")\n                //fmt.Println(\"------\")\n                continue\n            }\n            \n            if operationPerformed {\n                operationPerformed = false\n                continue\n            }\n        }*/\n    }\n    \n    fmt.Println(minOperations, maxOperations)\n}\n\n","offset":460,"fileType":"go","requestStartTime":1517914005837}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x1, v1, x2, v2 int\n    fmt.\n}\n\n","offset":78,"fileType":"go","requestStartTime":1517921344330}}
{"body": {"column":12,"line":17,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x1, v1, x2, v2 int\n    fmt.Scanf(\"%d %d %d %d\", &x1, &v1, &x2, &v2)\n    \n    landSameLocation := false\n    for i:=0;i<1000;i++ {\n        d1 := x1 + i*v1\n        d2 := x2 + i*v2\n        if d1 == d2 {\n            landSameLocation = true\n        }\n    }\n    if landSameLocation {\n        fmt.\n    }\n}\n\n","offset":340,"fileType":"go","requestStartTime":1517921480378}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var n int\n    fmt.\n}\n\n","offset":65,"fileType":"go","requestStartTime":1517921778140}}
{"body": {"column":12,"line":8,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var n int\n    fmt.Scanf(\"%d\", &n)\n    for i:=0;i<n;i++ {\n        fmt.\n    }\n}\n\n","offset":116,"fileType":"go","requestStartTime":1517921801942}}
{"body": {"column":8,"line":13,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var n int\n    var sum uint64\n    fmt.Scanf(\"%d\", &n)\n    for i:=0;i<n;i++ {\n        var val uint64\n        fmt.Scanf(\"%d\", &val)\n        sum += val\n    }\n    fmt.\n}\n\n","offset":209,"fileType":"go","requestStartTime":1517921845673}}
{"body": {"column":17,"line":9,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    io := buffer.\n}\n\n","offset":89,"fileType":"go","requestStartTime":1517924491843}}
{"body": {"column":16,"line":9,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    io := bufio.\n}\n\n","offset":88,"fileType":"go","requestStartTime":1517924494274}}
{"body": {"column":31,"line":9,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    io := bufio.NewScanner(&os.)\n}\n\n","offset":103,"fileType":"go","requestStartTime":1517924502858}}
{"body": {"column":12,"line":16,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var candles int\n    var highMap map[int]int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &candles)\n    \n    highMap = make(map[int]int)\n    for i := 0; i < candles; i++ {\n        fmt.\n    }\n}\n\n","offset":268,"fileType":"go","requestStartTime":1517924736506}}
{"body": {"column":8,"line":22,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var candles int\n    var highMap map[int]int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &candles)\n    \n    highMap = make(map[int]int)\n    for i := 0; i < candles; i++ {\n        var currentValue int\n        fmt.Fscan(io, &currentValue)\n        \n        highMap[currentValue]++\n    }\n    \n    fmt.\n}\n\n","offset":382,"fileType":"go","requestStartTime":1517924777396}}
{"body": {"column":16,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.\n}\n\n","offset":109,"fileType":"go","requestStartTime":1517925037589}}
{"body": {"column":29,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.)\n}\n\n","offset":122,"fileType":"go","requestStartTime":1517925042661}}
{"body": {"column":8,"line":11,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.\n}\n\n","offset":137,"fileType":"go","requestStartTime":1517925046598}}
{"body": {"column":8,"line":13,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.\n}\n\n","offset":171,"fileType":"go","requestStartTime":1517925086438}}
{"body": {"column":8,"line":13,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.\n}\n\n","offset":171,"fileType":"go","requestStartTime":1517925091039}}
{"body": {"column":8,"line":13,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.\n}\n\n","offset":171,"fileType":"go","requestStartTime":1517925099055}}
{"body": {"column":21,"line":13,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.Fprint(bufio.)\n}\n\n","offset":184,"fileType":"go","requestStartTime":1517925106320}}
{"body": {"column":34,"line":13,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.Fprint(bufio.NewWriter(os.))\n}\n\n","offset":197,"fileType":"go","requestStartTime":1517925111679}}
{"body": {"column":8,"line":14,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    wr := bufio.NewWriter(os.Stdout)\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    fmt.\n    fmt.Fprint())\n}\n\n","offset":208,"fileType":"go","requestStartTime":1517925130120}}
{"body": {"column":12,"line":14,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    for i := 0; i < students; i++ {\n        fmt.\n    }\n}\n\nfunc getCalculatedGrade(initialGrade int) int {\n    if initialGrade < 38 {\n        return initialGrade\n    }\n    \n    \n}\n\n","offset":211,"fileType":"go","requestStartTime":1517925439805}}
{"body": {"column":12,"line":14,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var students int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &students)\n    \n    for i := 0; i < students; i++ {\n        fmt.\n        fmt.Println(getCalculatedGrade())\n    }\n}\n\nfunc getCalculatedGrade(initialGrade int) int {\n    if initialGrade < 38 {\n        return initialGrade\n    }\n    \n    \n}\n\n","offset":211,"fileType":"go","requestStartTime":1517925464588}}
{"body": {"column":16,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var scores int\n    io := bufio.\n}\n\n","offset":107,"fileType":"go","requestStartTime":1517925902963}}
{"body": {"column":29,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var scores int\n    io := bufio.NewReader(os.)\n}\n\n","offset":120,"fileType":"go","requestStartTime":1517925906931}}
{"body": {"column":8,"line":34,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var highestScore, highScoreCounter int\n    var lowestScore, lowScoreCounter int\n    var scores int\n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &scores)\n    \n    for i := 0; i < scores; i++ {\n        var currentScore int\n        fmt.Fscan(io, &currentScore)\n        if i == 0 {\n            highestScore = currentScore\n            lowestScore = currentScore\n            continue\n        }\n        \n        if currentScore > highestScore {\n            highestScore = currentScore\n            highScoreCounter++\n        }\n        if currentScore < lowestScore {\n            lowestScore = currentScore\n            lowScoreCounter++\n        }\n    }\n    \n    fmt.\n}\n\n","offset":742,"fileType":"go","requestStartTime":1517926176765}}
{"body": {"column":16,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var listAItems, listBItems int\n    io := bufio.\n}\n\n","offset":123,"fileType":"go","requestStartTime":1517926608996}}
{"body": {"column":17,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 2\n    var b float64 = 2.0\n    var s string = \"sardor\"\n    // Read and save an integer, double, and String to your variables.\nscanner := bufio.\n    // Print the sum of both integer variables on a new line.\n    \n    // Print the sum of the double variables on a new line.\n    \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n\n}\n","offset":515,"fileType":"go","requestStartTime":1517843810270}}
{"body": {"column":29,"line":10,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var listAItems, listBItems int\n    io := bufio.NewReader(os.)\n}\n\n","offset":136,"fileType":"go","requestStartTime":1517926613652}}
{"body": {"column":8,"line":11,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var listAItems, listBItems int\n    io := bufio.NewReader(os.Stdin)\n    fmt.\n}\n\n","offset":151,"fileType":"go","requestStartTime":1517926618253}}
{"body": {"column":31,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 2\n    var b float64 = 2.0\n    var s string = \"sardor\"\n    // Read and save an integer, double, and String to your variables.\nscanner := bufio.NewScanner(os.)\n    // Print the sum of both integer variables on a new line.\n    \n    // Print the sum of the double variables on a new line.\n    \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n\n}\n","offset":529,"fileType":"go","requestStartTime":1517843817278}}
{"body": {"column":8,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 2\n    var b float64 = 2.0\n    var s string = \"sardor\"\n    // Read and save an integer, double, and String to your variables.\nscanner := bufio.NewScanner(os.Stdin)\n    // Print the sum of both integer variables on a new line.\n    fmt.\n    // Print the sum of the double variables on a new line.\n    \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n\n}\n","offset":606,"fileType":"go","requestStartTime":1517843834784}}
{"body": {"column":12,"line":19,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var listASize, listBSize int\n    var listAItems, listBItems []int\n    \n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &listASize, &listBSize)\n    \n    listAItems = make([]int, listASize)\n    listBItems = make([]int, listBSize)\n    \n    for i := 0; i < listASize; i++ {\n        fmt.\n    }\n}\n\n","offset":364,"fileType":"go","requestStartTime":1517926756441}}
{"body": {"column":10,"line":26,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n)\n\nfunc main() {\n    var listASize, listBSize int\n    var listAItems, listBItems []int\n    \n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &listASize, &listBSize)\n    \n    listAItems = make([]int, listASize)\n    listBItems = make([]int, listBSize)\n    \n    for i := 0; i < listASize; i++ {\n        fmt.Fscan(io, &listAItems[i])\n    }\n    \n    for i := 0; i < listBSize; i++ {\n        fmt.Fscan(io, &listBItems[i])\n    }\n    \n    sorts.\n}\n\n","offset":497,"fileType":"go","requestStartTime":1517926926751}}
{"body": {"column":12,"line":30,"wordToComplete":"","code":"\npackage main\nimport (\n    \"os\"\n    \"fmt\"\n    \"bufio\"\n    \"sort\"\n)\n\nfunc main() {\n    var listASize, listBSize int\n    var listAItems, listBItems []int\n    \n    io := bufio.NewReader(os.Stdin)\n    fmt.Fscan(io, &listASize, &listBSize)\n    \n    listAItems = make([]int, listASize)\n    listBItems = make([]int, listBSize)\n    \n    for i := 0; i < listASize; i++ {\n        fmt.Fscan(io, &listAItems[i])\n    }\n    \n    for i := 0; i < listBSize; i++ {\n        fmt.Fscan(io, &listBItems[i])\n    }\n    sort.Ints(listAItems)\n\tsort.Ints(listBItems)\n        \n    for _, item := range listAItems {\n        fmt.\n    }\n    \n    fmt.Println(listAItems)\n    fmt.Println(listBItems)\n}\n\n","offset":601,"fileType":"go","requestStartTime":1517927053419}}
{"body": {"column":4,"line":28,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 2\n    var b float64 = 2.0\n    var x string = \"is the best place to learn and practice coding!\"\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nfmt.(i + j)\n    // Print the sum of the double variables on a new line.\n    fmt.Fprintln(d + b)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n    fmt.Fprintln(s + \" \" + x)\n}\n","offset":635,"fileType":"go","requestStartTime":1517845471637}}
{"body": {"column":8,"line":28,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 2\n    var b float64 = 2.0\n    var x string = \"is the best place to learn and practice coding!\"\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.\n    // Print the sum of the double variables on a new line.\n    fmt.Println(d)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n    fmt.Println(x)\n}\n","offset":639,"fileType":"go","requestStartTime":1517845539665}}
{"body": {"column":8,"line":29,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.\n    // Print the sum of the double variables on a new line.\n    fmt.Println(d)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n    fmt.Println(x)\n}\n","offset":641,"fileType":"go","requestStartTime":1517845651261}}
{"body": {"column":8,"line":29,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.\n    // Print the sum of the double variables on a new line.\n    fmt.Println(d)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n    fmt.Println(x)\n}\n","offset":641,"fileType":"go","requestStartTime":1517846145410}}
{"body": {"column":17,"line":29,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner := bufio.\n    // Print the sum of the double variables on a new line.\nscanner.Text(b)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Text(x)\n}\n","offset":650,"fileType":"go","requestStartTime":1517846247102}}
{"body": {"column":35,"line":29,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner := bufio.NewScanner(uint64.)\n    // Print the sum of the double variables on a new line.\nscanner.Text(b)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Text(x)\n}\n","offset":668,"fileType":"go","requestStartTime":1517846278816}}
{"body": {"column":4,"line":30,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner := bufio.NewScanner(uint64.NewReader(j))\nfmt.\n    // Print the sum of the double variables on a new line.\nscanner.Text(b)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Text(x)\n}\n","offset":686,"fileType":"go","requestStartTime":1517846294700}}
{"body": {"column":20,"line":30,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner := bufio.NewScanner(uint64.NewReader(j))\nfmt.Println(scanner.)\n    // Print the sum of the double variables on a new line.\nscanner.Text(b)\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Text(x)\n}\n","offset":702,"fileType":"go","requestStartTime":1517846313285}}
{"body": {"column":17,"line":23,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\nscanner := bufio.\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n    // Print the sum of the double variables on a new line.\nfmt.Println(scanner.Text())\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(scanner.Text())\n}\n","offset":486,"fileType":"go","requestStartTime":1517846723583}}
{"body": {"column":8,"line":29,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.\nfor scanner.Scan() {\n\t\tfmt.Println(scanner.Text()) // Println will add back the final '\\n'\n\t}\n    // Print the sum of the double variables on a new line.\n\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n\n}\n","offset":641,"fileType":"go","requestStartTime":1517846876352}}
{"body": {"column":8,"line":18,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\nscanner.\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\n\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\n\n}\n","offset":300,"fileType":"go","requestStartTime":1517847005336}}
{"body": {"column":12,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    scanner.\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(scanner.Text()) \n}\n","offset":482,"fileType":"go","requestStartTime":1517847243997}}
{"body": {"column":17,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\nscanner := bufio.\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.Scan()\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\nscanner.Scan()\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Scan()\nfmt.Println(scanner.Text()) \n}\n","offset":487,"fileType":"go","requestStartTime":1517847303474}}
{"body": {"column":31,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\nscanner := bufio.NewScanner(os.)\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.Scan()\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\nscanner.Scan()\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Scan()\nfmt.Println(scanner.Text()) \n}\n","offset":501,"fileType":"go","requestStartTime":1517847312763}}
{"body": {"column":31,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\nscanner := bufio.NewScanner(os.)\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.Scan()\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\nscanner.Scan()\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Scan()\nfmt.Println(scanner.Text()) \n}\n","offset":501,"fileType":"go","requestStartTime":1517847340224}}
{"body": {"column":31,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\nscanner := bufio.NewScanner(os.)\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.Scan()\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\nscanner.Scan()\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Scan()\nfmt.Println(scanner.Text()) \n}\n","offset":501,"fileType":"go","requestStartTime":1517847343780}}
{"body": {"column":8,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nscanner.\nb = b + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nscanner.Scan()\n\t\tfmt.Println(scanner.Text()) \n    // Print the sum of the double variables on a new line.\nscanner.Scan()\n    fmt.Println(scanner.Text()) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nscanner.Scan()\nfmt.Println(scanner.Text()) \n}\n","offset":559,"fileType":"go","requestStartTime":1517847468047}}
{"body": {"column":8,"line":30,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj =  + i\n\nb =  + d\nx = s + \n    // Print the sum of both integer variables on a new line.\n    fmt.\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":639,"fileType":"go","requestStartTime":1517847847796}}
{"body": {"column":20,"line":30,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj =  + i\n\nb =  + d\nx = s + \n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.)\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":651,"fileType":"go","requestStartTime":1517847857906}}
{"body": {"column":8,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nj = j + i\nscanner.\nb = d + d\nx = s + x\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":559,"fileType":"go","requestStartTime":1517847957657}}
{"body": {"column":8,"line":24,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n   \n\n    // Read and save an integer, double, and String to your variables.\nj := j + i\nscanner.\nb := d + d\n\nx := s + x\nscanner.Scan()\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":449,"fileType":"go","requestStartTime":1517848040181}}
{"body": {"column":8,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n   \n\n    // Read and save an integer, double, and String to your variables.\nj := j + i\nscanner.Scan()\nb := d + d\nscanner.\nx := s + x\nscanner.Scan()\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":475,"fileType":"go","requestStartTime":1517848046170}}
{"body": {"column":8,"line":25,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.\nj = j + i\n\nb = d + d\n\nx = s + x\nscanner.Scan()\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":549,"fileType":"go","requestStartTime":1517848099453}}
{"body": {"column":8,"line":25,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.\nj = j + i\n\nb = d + d\n\nx = s + x\nscanner.Scan()\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":549,"fileType":"go","requestStartTime":1517848121799}}
{"body": {"column":8,"line":25,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.\nj = j + i\n\nb = d + d\n\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":549,"fileType":"go","requestStartTime":1517848132891}}
{"body": {"column":8,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.Scan()\nscanner.\nj = j + i\n\nb = d + d\n\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":564,"fileType":"go","requestStartTime":1517848137651}}
{"body": {"column":20,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.Scan()\nscanner.Split(bufio.)\nj = j + i\n\nb = d + d\n\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(scanner.Text())\n\t\tfmt.Println(j) \n    // Print the sum of the double variables on a new line.\n    fmt.Println(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":576,"fileType":"go","requestStartTime":1517848201526}}
{"body": {"column":13,"line":26,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\n    var b float64 = 4.0\n    var x string = \"is the best place to learn and practice coding!\"\n\n    // Read and save an integer, double, and String to your variables.\nscanner.Scan()\np := scanner.\nj = p + i\nscanner.Scan()\nq := scanner.Text()\nb = d + d\nscanner.Scan()\nr := scanner.Text()\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(p)\n    // Print the sum of the double variables on a new line.\n    fmt.Println(q) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(r) \n}\n","offset":569,"fileType":"go","requestStartTime":1517848388112}}
{"body": {"column":8,"line":37,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\nscanner.Scan()  \n    var b float64 = 4.0\nscanner.Scan()\n    var x string = \"is the best place to learn and practice coding!\"\nscanner.Scan()\n\n    // Read and save an integer, double, and String to your variables.\n\n\nj = j + i\nb = b + d\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(j)\n    // Print the sum of the double variables on a new line.\n    fmt.(b) \n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":766,"fileType":"go","requestStartTime":1517848484872}}
{"body": {"column":4,"line":38,"wordToComplete":"","code":"package main\n\n\nimport (\n  \"fmt\"\n  \"os\"\n  \"bufio\"\n  \"strconv\"\n)\n\nfunc main() {\n  \tvar _ = strconv.Itoa // Ignore this comment. You can still use the package \"strconv\".\n  \n    var i uint64 = 4\n    var d float64 = 4.0\n    var s string = \"HackerRank \"\n\n    scanner := bufio.NewScanner(os.Stdin)\n\n    // Declare second integer, double, and String variables.\n    var j uint64 = 12\nscanner.Scan()  \n    var b float64 = 4.0\nscanner.Scan()\n    var x string = \"is the best place to learn and practice coding!\"\nscanner.Scan()\n\n    // Read and save an integer, double, and String to your variables.\n\n\nj = j + i\nb = b + d\nx = s + x\n\n    // Print the sum of both integer variables on a new line.\nfmt.Println(j)\n    // Print the sum of the double variables on a new line.\n    fmt.Printf(\"%b\", b) \nfmt.\n    // Concatenate and print the String variables on a new line\n    // The 's' variable above should be printed first.\nfmt.Println(x) \n}\n","offset":787,"fileType":"go","requestStartTime":1517848641866}}
{"body": {"column":17,"line":8,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n-\n    var len\n    _, len = fmt.\n}\n\n","offset":145,"fileType":"go","requestStartTime":1517931784030}}
{"body": {"column":12,"line":13,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n\n    var len int\n    fmt.Scanln(\"%d\", &len)\n    var elem int\n    \n    sum := 0\n    for i := 0; i < len;i++ {\n        fmt.Scan(\"%d \", &elem)\n        sum += elem\n    }\n\n}\n\n","offset":235,"fileType":"go","requestStartTime":1517933177644}}
{"body": {"column":8,"line":16,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n\n    var len int\n    fmt.Scanln(\"%d\", &len)\n    var elem int\n    \n    sum := 0\n    for i := 0; i < len;i++ {\n        fmt.Scan(\"%d \", &elem)\n        sum += elem\n    }\n    fmr.\n}\n\n","offset":288,"fileType":"go","requestStartTime":1517933213485}}
{"body": {"column":8,"line":16,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n\n    var len int\n    fmt.Scanln(\"%d\", &len)\n    var elem int\n    \n    sum := 0\n    for i := 0; i < len;i++ {\n        fmt.Scan(\"%d \", &elem)\n        sum += elem\n    }\n    fmt.\n}\n\n","offset":288,"fileType":"go","requestStartTime":1517933214199}}
{"body": {"column":29,"line":81,"wordToComplete":"","code":"\npackage main\n\nimport (\n    \"bufio\"       \n    \"fmt\"\n    \"log\"\n    \"os\"\n)\n\nvar (\n    in *bufio.Reader\n    out *bufio.Writer\n) \n\nfunc input(f string, ps ...interface{}) {\n    _, err := fmt.Fscanf(in, f, ps...) \n    if err != nil {\n        log.Fatal(err)\n    }\n}\n\nfunc output(f string, vs ...interface{}) {\n    _, err := fmt.Fprintf(out, f, vs...) \n    if err != nil {\n        log.Fatal(err)\n    }\n}\n\ntype graph struct {\n    m map[int]map[int]int\n}\n\nfunc newGraph() *graph{\n    return &graph{make(map[int]map[int]int)} \n}\n\nfunc (g *graph) put(u,v,d int) {\n    g.puts(u, v, d)\n    g.puts(v, u, d)\n}\n\nfunc (g *graph) puts(u,v,d int) {\n    m, ok := g.m[u]\n    if !ok {\n        m = make(map[int]int)\n        g.m[u] = m\n    }\n    m[v] = d\n}\n\nfunc (g *graph) get(u,v int) int {\n    m, ok := g.m[u]\n    if !ok {\n        return -1    \n    }\n    d, ok := m[v]\n    if !ok {\n        return -1    \n    }\n    return d\n}\n\nfunc (g *graph) children(u int) (cs []int) {\n    for v := range g.m[u] {\n        cs = append(cs,v) \n    }\n    return\n}\n\nfunc (g *graph) calc(s int) (d *dist) {\n    d = newDist() \n    q := newQueue() \n    q.put(s, 0)\n    output(\"%#v\\n\",g)\n    for !q.empty() {\n        output(\"%#v\\n%#v\\n\",q,d)\n        u, l := q.get() \n        d.put(u, l)\n        for v := range g.children(u) {\n            if !d.has(v) {\n                q.put(v, l+g.)\n            }\n        }\n    } \n    return \n}\n\ntype queue struct {\n    v []int\n    d []int\n}\n\nfunc newQueue() *queue {\n    return &queue{} \n}\n\nfunc (q *queue) put(u, d int) {\n    q.v = append(q.v, u) \n    q.d = append(q.d, d) \n}\n\nfunc (q *queue) get() (u, d int) {\n    u = q.v[0]\n    d = q.d[0]\n    q.v = q.v[1:]\n    q.d = q.d[1:]\n    return\n}\n\nfunc (q *queue) empty() bool {\n    return len(q.v) == 0\n}\n\ntype dist struct {\n    m map[int]int\n}\n\nfunc newDist() *dist{\n    return &dist{make(map[int]int)} \n}\n\nfunc (d *dist) put(u, l int) {\n    d.m[u] = l\n}\n\nfunc (d *dist) get(u int) int {\n    l, ok := d.m[u]\n    if !ok {\n        return -1\n    }\n    return l\n}\n\nfunc (d *dist) has(u int) bool {\n    _, ok := d.m[u]\n    return ok\n}\n\nfunc main() {\n    in = bufio.NewReader(os.Stdin)\n    out = bufio.NewWriter(os.Stdout)\n    defer out.Flush()\n    \n    var q int \n    input(\"%d\\n\", &q)\n    for ; q > 0; q-- {\n        g := newGraph() \n        var n, m int\n        input(\"%d %d\\n\", &n, &m)\n        for i := 0 ; i < m; i++ {\n            var u, v int\n            input(\"%d %d\\n\", &u, &v)\n            g.put(u,v,6)\n        }\n        var s int\n        input(\"%d\\n\", &s)\n        d := g.calc(s) \n        for u := 1 ; u <= n; u++ {\n            if u != s {\n                output(\"%d \", d.get(u))\n            }\n        }\n        output(\"\\n\")\n    }\n}\n\n\n\n","offset":1339,"fileType":"go","requestStartTime":1517958808772}}
{"body": {"column":30,"line":6,"wordToComplete":"Hello, World.\"","code":"\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    fmt.Println(\"Hello, World.\")\n}\n\n","offset":74,"fileType":"go","requestStartTime":1517966553867}}
{"body": {"column":27,"line":7,"wordToComplete":"","code":"\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n    inputString := scanner.\n\n    fmt.Println(\"Hello, World.\")\n}\n\n","offset":113,"fileType":"go","requestStartTime":1517966997117}}
{"body": {"column":12,"line":11,"wordToComplete":"","code":"\npackage main\n\nimport (\n    \"fmt\"\n    \"bufio\"\n    \"os\"\n)\n\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n    scanner.\n    inputString := scanner.Text()\n\n    fmt.Println(\"Hello, World.\")\n    fmt.Println(inputString)\n}\n\n","offset":127,"fileType":"go","requestStartTime":1517969077005}}
{"body": {"column":12,"line":7,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var arrayElements int\n    for i := 0, i < arrayElements, i++ {\n        fmt.\n    }\n}\n\n","offset":122,"fileType":"go","requestStartTime":1517971564735}}
{"body": {"column":8,"line":7,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var arrayElements int\n    \n    fmt.\n    \n    for i := 0, i < arrayElements, i++ {\n        fmt.Scan\n    }\n}\n\n","offset":82,"fileType":"go","requestStartTime":1517971575590}}
{"body": {"column":8,"line":14,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var arrayElements, nextNumber, sum int\n    \n    fmt.Scan(&arrayElements)\n    \n    for i := 0, i < arrayElements, i++ {\n        fmt.Scan(&nextNumber)\n        sum += nextNumber\n    }\n    \n    fmt.\n}\n\n","offset":241,"fileType":"go","requestStartTime":1517971650323}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var arrayLength int\n    fmt.\n}\n\n","offset":75,"fileType":"go","requestStartTime":1517972581005}}
{"body": {"column":8,"line":8,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var arrayLength int\n    fmt.Scan(&arrayLength)\n    \n    fmt.\n}\n\n","offset":107,"fileType":"go","requestStartTime":1517972592739}}
{"body": {"column":21,"line":5,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    scanner := bufio.\n    \n    \n}\n\n","offset":64,"fileType":"go","requestStartTime":1517974281958}}
{"body": {"column":35,"line":5,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    scanner := bufio.NewScanner(os.)\n    \n    \n}\n\n","offset":78,"fileType":"go","requestStartTime":1517974288167}}
{"body": {"column":16,"line":11,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"bufio\"\n    \"os\"\n)\n\nfunc main() {\n    scanner := bufio.NewScanner(os.Stdin)\n    \n    for scanner.\n    \n}\n\n","offset":135,"fileType":"go","requestStartTime":1517974419297}}
{"body": {"column":12,"line":9,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    //Enter your code here. Read input from STDIN. Print output to STDOUT\n    var n int\n    fmt.Scanf(\"%d\", &n)\n    for i:=0;i<n;i++ {\n        fmt.\n    }\n}\n\n","offset":190,"fileType":"go","requestStartTime":1517982531513}}
{"body": {"column":12,"line":65,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n    var n, kx, ky, px, size, count, i1, i2, rez int\n    var s string\n    fmt.Scanf(\"%d\", &n)\n    size = 8*8\n    for z:=0;z<n;z++ {\n        nums := make([]int, size)\n        for i:=0;i<8; i++ {\n            fmt.Scanf(\"%s\", s)\n            for j,c := range s {\n                if c == 'k' {\n                    kx = j\n                    ky = i\n                    nums[i*len(s) + j] = 0\n                }\n                if i == 1 && c == 'P' {\n                    px = j\n                }\n                if c == 'Q' || c == 'q' || c == 'K' || c == 'N' || c == 'n' || c == 'B' || c == 'b' || c == 'r' || c == 'R' || c == 'p' {\n                    nums[i*len(s) + j] = 1\n                }\n            }\n        }\n        \n        if kx == px {\n            count = 0\n            for p:=kx; p<8*ky + kx; p=p+8 {\n                if nums[p] == 1 {\n                    count ++\n                } \n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        if ky == 0 {\n            count = 0\n            if kx>px {\n                for p:=px;p<kx;p++ {\n                    if nums[p] == 1 {\n                        count++\n                    }\n                } \n            } else {\n                for p:=kx;p<px;p++ {\n                    if nums[p] == 1 {\n                        count++\n                    }\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        if ky == 1 && (px - kx == 2 || px - kx == -2) {\n            rez = 1\n        } \n        if ky == 2 && (px - kx == 1 || px - kx == -1) {\n            rez = 1\n        }\n        fmt.\n        if kx>px && ky == kx-px {\n            i2 = 1\n            for p:=px; p<8*ky;p++ {\n                if p == i2*9 {\n                    if nums[p] == 1{\n                        count ++\n                    }\n                    i2++\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        } \n        if kx<px && ky == px-kx {\n            count = 0\n            i1 = 1\n            for p:=px; p<8*ky; p++ {\n                if p == i1*7 {\n                    if nums[p] == 1{\n                        count ++\n                    }\n                    i1++\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        fmt.Println(rez)\n    }\n}\n\n","offset":1750,"fileType":"go","requestStartTime":1518007327097}}
{"body": {"column":12,"line":79,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n //Enter your code here. Read input from STDIN. Print output to STDOUT\n    var n, kx, ky, px, size, count, i1, i2, rez int\n    var s string\n    fmt.Scanf(\"%d\", &n)\n    size = 8*8\n    for z:=0;z<n;z++ {\n        nums := make([]int, size)\n        for i:=0;i<8; i++ {\n            fmt.Scanf(\"%s\", &s)\n            for j,c := range s {\n                if c == 'k' {\n                    kx = j\n                    ky = i\n                    nums[i*len(s) + j] = 0\n                }\n                if i == 1 && c == 'P' {\n                    px = j\n                }\n                if c == 'Q' || c == 'q' || c == 'K' || c == 'N' || c == 'n' || c == 'B' || c == 'b' || c == 'r' || c == 'R' || c == 'p' {\n                    nums[i*len(s) + j] = 1\n                }\n            }\n        }\n        \n        if kx == px {\n            count = 0\n            for p:=kx; p<8*ky + kx; p=p+8 {\n                if nums[p] == 1 {\n                    count ++\n                } \n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        if ky == 0 {\n            count = 0\n            if kx>px {\n                for p:=px;p<kx;p++ {\n                    if nums[p] == 1 {\n                        count++\n                    }\n                } \n            } else {\n                for p:=kx;p<px;p++ {\n                    if nums[p] == 1 {\n                        count++\n                    }\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        if ky == 1 && (px - kx == 2 || px - kx == -2) {\n            rez = 1\n        } \n        if ky == 2 && (px - kx == 1 || px - kx == -1) {\n            rez = 1\n        }\n        if kx>px && ky == kx-px {\n            i2 = 1\n            for p:=px; p<8*ky;p++ {\n                if p == i2*9 {\n                    if nums[p] == 1{\n                        count ++\n                    }\n                    i2++\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        } \n        fmt.\n        if kx<px && ky == px-kx {\n            \n            //fmt.Printf(\"%d %d %d\\n\", kx, px, ky)\n            count = 0\n            i1 = 1\n            for p:=px; p<8*ky; p++ {\n                fmt.Printf(\"%d %d\\n\", p, nums[p])\n                if p == i1*7 {\n                    if nums[p] == 1{\n                        count ++\n                    }\n                    i1++\n                }\n            }\n            if count == 0 {\n                rez = 2\n            }\n        }\n        fmt.Println(rez)\n    }\n}\n\n","offset":2097,"fileType":"go","requestStartTime":1518007840475}}
{"body": {"column":9,"line":21,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"os\"\n    \"bufio\"\n    \"strings\"\n    \"strconv\"\n)\n\nfunc main() {\n    r := bufio.NewReader(os.Stdin)\n    str1, _ := r.ReadString('\\n')\n    str1 = strings.TrimSpace(str1)\n    n,_ := strconv.Atoi(str1)\n    sl:= make([]string,n)\n    for i := 0; i < ln; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        sl[i]=s\n    }\n    sort.\n    str2, _ := r.ReadString('\\n')\n    f2 := strings.Fields(str2)\n    lq,_ := strconv.Atoi(f2[0])\n    for i := 0; i < lq; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        fmt.Println(m[s])\n    }\n}\n\n","offset":387,"fileType":"go","requestStartTime":1518009561408}}
{"body": {"column":8,"line":22,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"os\"\n    \"bufio\"\n    \"strings\"\n    \"strconv\"\n    \"sort\"\n)\n\nfunc main() {\n    r := bufio.NewReader(os.Stdin)\n    str1, _ := r.ReadString('\\n')\n    str1 = strings.TrimSpace(str1)\n    n,_ := strconv.Atoi(str1)\n    sl:= make([]string,n)\n    for i := 0; i < ln; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        sl[i]=s\n    }\n    sor.\n    str2, _ := r.ReadString('\\n')\n    f2 := strings.Fields(str2)\n    lq,_ := strconv.Atoi(f2[0])\n    for i := 0; i < lq; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        fmt.Println(m[s])\n    }\n}\n\n","offset":397,"fileType":"go","requestStartTime":1518009571371}}
{"body": {"column":9,"line":22,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"os\"\n    \"bufio\"\n    \"strings\"\n    \"strconv\"\n    \"sort\"\n)\n\nfunc main() {\n    r := bufio.NewReader(os.Stdin)\n    str1, _ := r.ReadString('\\n')\n    str1 = strings.TrimSpace(str1)\n    n,_ := strconv.Atoi(str1)\n    sl:= make([]string,n)\n    for i := 0; i < ln; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        sl[i]=s\n    }\n    sort.\n    str2, _ := r.ReadString('\\n')\n    f2 := strings.Fields(str2)\n    lq,_ := strconv.Atoi(f2[0])\n    for i := 0; i < lq; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        fmt.Println(m[s])\n    }\n}\n\n","offset":398,"fileType":"go","requestStartTime":1518009572441}}
{"body": {"column":12,"line":24,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"os\"\n    \"bufio\"\n    \"strings\"\n    \"strconv\"\n    \"sort\"\n)\n\nfunc main() {\n    r := bufio.NewReader(os.Stdin)\n    str1, _ := r.ReadString('\\n')\n    str1 = strings.TrimSpace(str1)\n    n,_ := strconv.Atoi(str1)\n    sl:= make([]string,n)\n    for i := 0; i < ln; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        sl[i]=s\n    }\n    sort.Strings(sl)\n    for _, s := range sl{\n        fmt.\n    }\n}\n\n","offset":448,"fileType":"go","requestStartTime":1518009599960}}
{"body": {"column":24,"line":20,"wordToComplete":"","code":"\npackage main\nimport (\n    \"fmt\"\n    \"os\"\n    \"bufio\"\n    \"strings\"\n    \"strconv\"\n    \"sort\"\n)\n\nfunc main() {\n    r := bufio.NewReader(os.Stdin)\n    str1, _ := r.ReadString('\\n')\n    str1 = strings.TrimSpace(str1)\n    n,_ := strconv.Atoi(str1)\n    sl:= make([]int,n)\n    for i := 0; i < n; i++{\n        s,_ := r.ReadString('\\n')\n        s = strings.TrimSpace(s)\n        in,_ := strconv.\n        sl[i]=s\n    }\n    sort.Strings(sl)\n    for _, s := range sl{\n        fmt.Println(s)\n    }\n}\n\n","offset":387,"fileType":"go","requestStartTime":1518009780213}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.\n}\n\n","offset":68,"fileType":"go","requestStartTime":1518016329407}}
{"body": {"column":8,"line":8,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", &x)\n    \n    fmt.\n}\n\n","offset":97,"fileType":"go","requestStartTime":1518016352117}}
{"body": {"column":18,"line":6,"wordToComplete":"s.\"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s.\", &x)\n    \n    fmt.Println(x)\n}\n\n","offset":78,"fileType":"go","requestStartTime":1518016491352}}
{"body": {"column":19,"line":6,"wordToComplete":"s..\"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s..\", &x)\n    \n    fmt.Println(x)\n}\n\n","offset":79,"fileType":"go","requestStartTime":1518016491518}}
{"body": {"column":20,"line":6,"wordToComplete":"s...\"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s...\", &x)\n    \n    fmt.Println(x)\n}\n\n","offset":80,"fileType":"go","requestStartTime":1518016491670}}
{"body": {"column":23,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", &x.)\n    \n    fmt.Println(x)\n}\n\n","offset":83,"fileType":"go","requestStartTime":1518016506460}}
{"body": {"column":24,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", &x..)\n    \n    fmt.Println(x)\n}\n\n","offset":84,"fileType":"go","requestStartTime":1518016506658}}
{"body": {"column":25,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", &x...)\n    \n    fmt.Println(x)\n}\n\n","offset":85,"fileType":"go","requestStartTime":1518016506841}}
{"body": {"column":21,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", .&x)\n    \n    fmt.Println(x)\n}\n\n","offset":81,"fileType":"go","requestStartTime":1518016522592}}
{"body": {"column":22,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", ..&x)\n    \n    fmt.Println(x)\n}\n\n","offset":82,"fileType":"go","requestStartTime":1518016522760}}
{"body": {"column":23,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var x string\n    fmt.Scanf(\"%s\", ...&x)\n    \n    fmt.Println(x)\n}\n\n","offset":83,"fileType":"go","requestStartTime":1518016522957}}
{"body": {"column":18,"line":34,"wordToComplete":"3f\\n\"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var n int //array size\n    var num int //number array\n    var positiveNum int\n    var negativeNum int\n    var zeroNum int\n    var x []int\n    fmt.Scan(&n)\n    fmt.Println(n)\n    \n    //Массив всех чисел\n    for i:=0; i<n; i++{\n        fmt.Scan(&num)\n        x = append(x,num)\n    }\n    fmt.Println(x)\n    \n    \n    \n    //Считает числа с разными знаками\n    for _, value := range(x){\n        if value > 0 {\n            positiveNum++\n        }else if value < 0{\n            negativeNum++\n        }else{\n            zeroNum++\n        }\n    }\n    //positiveNumFloat := float64(positiveNum)\n    fmt.Printf(\"%.3f\\n\",float64(positiveNum))\n    fmt.Println(negativeNum)\n    fmt.Println(zeroNum)\n\n}\n\n\n","offset":652,"fileType":"go","requestStartTime":1518017380912}}
{"body": {"column":8,"line":6,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var n int\n    fmt.\n}\n\n","offset":65,"fileType":"go","requestStartTime":1518018064971}}
{"body": {"column":8,"line":7,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var length, k int\n    var input string\n    f,t.\n}\n\n","offset":94,"fileType":"go","requestStartTime":1518018123043}}
{"body": {"column":8,"line":7,"wordToComplete":"","code":"\npackage main\nimport \"fmt\"\n\nfunc main() {\n    var length, k int\n    var input string\n    fmt.\n}\n\n","offset":94,"fileType":"go","requestStartTime":1518018124207}}
